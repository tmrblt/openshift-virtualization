---
# Required workflow variables:
#   project_name
#   db_name
#   vm_type
#
# actual_user should normally be published by a previous workflow node.
# When it is unavailable, awx_user_name is used as a fallback.
#
# Allowed vm_type values:
#   empty
#   postgresql13
#   mongodb83

- name: Step 3 - Provision Database Virtual Machine
  hosts: localhost
  connection: local
  gather_facts: false

  vars:
    template_namespace: "template"

    selected_vm_type: >-
      {{
        vm_type
        | default('empty')
        | string
        | lower
        | trim
      }}

    workflow_actual_user: >-
      {{
        actual_user
        | default(awx_user_name | default('', true), true)
        | string
        | trim
      }}

    ssh_username: "fedora"

    database_templates:
      postgresql13:
        template_name: "postgresql-13"
        vm_prefix: "postgresql-13"
        database_label: "postgresql13"

      mongodb83:
        template_name: "mongodb-83"
        vm_prefix: "mongodb-83"
        database_label: "mongodb83"

  tasks:
    - name: Validate virtual machine selection
      ansible.builtin.assert:
        that:
          - selected_vm_type in ['empty', 'postgresql13', 'mongodb83']
        fail_msg: >-
          Invalid vm_type value. Allowed values are empty, postgresql13,
          and mongodb83.

    - name: Display empty selection result
      ansible.builtin.debug:
        msg:
          - "No database virtual machine was selected."
          - "Namespace preparation completed successfully."
      when: selected_vm_type == 'empty'

    - name: Publish empty selection result
      ansible.builtin.set_stats:
        data:
          database_vm_created: false
          database_vm_type: "empty"
          database_vm_project: "{{ project_name | default('') }}"
          database_vm_owner: "{{ workflow_actual_user }}"
          database_vm_message: "No database virtual machine was requested."
        per_host: false
      when: selected_vm_type == 'empty'

    - name: End play for empty selection
      ansible.builtin.meta: end_play
      when: selected_vm_type == 'empty'

    - name: Validate required provisioning inputs
      ansible.builtin.assert:
        that:
          - project_name is defined
          - project_name | string | trim | length > 0
          - db_name is defined
          - db_name | string | trim | length > 0
          - workflow_actual_user | length > 0
        fail_msg: >-
          project_name, db_name, and actual_user are required.
          actual_user must be published by a previous workflow node or
          awx_user_name must be available.

    - name: Normalize database name
      ansible.builtin.set_fact:
        normalized_db_name: >-
          {{
            db_name
            | string
            | lower
            | trim
            | regex_replace('[^a-z0-9-]', '-')
            | regex_replace('-+', '-')
            | regex_replace('^-+', '')
            | regex_replace('-+$', '')
          }}

    - name: Normalize owner label value
      ansible.builtin.set_fact:
        owner_label: >-
          {{
            workflow_actual_user
            | lower
            | regex_replace('[^a-z0-9_.-]', '-')
            | regex_replace('-+', '-')
            | regex_replace('^[^a-z0-9]+', '')
            | regex_replace('[^a-z0-9]+$', '')
          }}

    - name: Select database template
      ansible.builtin.set_fact:
        selected_template: "{{ database_templates[selected_vm_type] }}"

    - name: Build resource names
      ansible.builtin.set_fact:
        vm_name: "{{ selected_template.vm_prefix }}-{{ normalized_db_name }}"
        cloud_init_secret_name: >-
          {{ selected_template.vm_prefix }}-{{ normalized_db_name }}-cloud-init
        ssh_service_name: >-
          {{ selected_template.vm_prefix }}-{{ normalized_db_name }}-ssh

    - name: Validate generated values
      ansible.builtin.assert:
        that:
          - normalized_db_name | length > 0
          - owner_label | length > 0
          - vm_name | length <= 63
          - cloud_init_secret_name | length <= 63
          - ssh_service_name | length <= 63
          - vm_name is match('^[a-z0-9]([-a-z0-9]*[a-z0-9])?$')
        fail_msg: >-
          A generated resource name or owner label is invalid.
          Use a shorter db_name containing letters, numbers, or hyphens.

    - name: Generate SSH password
      ansible.builtin.set_fact:
        ssh_password: >-
          {{
            lookup(
              'ansible.builtin.password',
              '/dev/null',
              length=20,
              chars=['ascii_letters', 'digits']
            )
          }}
      no_log: true

    - name: Verify target namespace exists
      kubernetes.core.k8s_info:
        api_version: v1
        kind: Namespace
        name: "{{ project_name }}"
      register: target_namespace

    - name: Fail when target namespace does not exist
      ansible.builtin.assert:
        that:
          - target_namespace.resources | length == 1
        fail_msg: >-
          Target namespace {{ project_name }} does not exist.
          Run the namespace creation workflow steps first.

    - name: Read selected virtual machine template
      kubernetes.core.k8s_info:
        api_version: template.openshift.io/v1
        kind: Template
        namespace: "{{ template_namespace }}"
        name: "{{ selected_template.template_name }}"
      register: source_template

    - name: Validate selected template
      ansible.builtin.assert:
        that:
          - source_template.resources | length == 1
          - source_template.resources[0].objects is defined
          - source_template.resources[0].objects | length > 0
        fail_msg: >-
          Template {{ template_namespace }}/{{ selected_template.template_name }}
          was not found or does not contain a VirtualMachine object.

    - name: Select VirtualMachine object from template
      ansible.builtin.set_fact:
        template_vm_object: >-
          {{
            source_template.resources[0].objects
            | selectattr('kind', 'equalto', 'VirtualMachine')
            | list
            | first
          }}

    - name: Validate VirtualMachine template object
      ansible.builtin.assert:
        that:
          - template_vm_object is defined
          - template_vm_object.kind == 'VirtualMachine'
        fail_msg: >-
          Template {{ template_namespace }}/{{ selected_template.template_name }}
          does not contain a valid VirtualMachine object.

    - name: Check for an existing virtual machine
      kubernetes.core.k8s_info:
        api_version: kubevirt.io/v1
        kind: VirtualMachine
        namespace: "{{ project_name }}"
        name: "{{ vm_name }}"
      register: existing_vm

    - name: Prevent replacement of an existing virtual machine
      ansible.builtin.assert:
        that:
          - existing_vm.resources | length == 0
        fail_msg: >-
          VirtualMachine {{ project_name }}/{{ vm_name }} already exists.
          Choose a different db_name or remove the existing virtual machine.

    - name: Create cloud-init secret
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: Secret
          metadata:
            name: "{{ cloud_init_secret_name }}"
            namespace: "{{ project_name }}"
            labels:
              app: "{{ vm_name }}"
              app.kubernetes.io/name: "{{ vm_name }}"
              app.kubernetes.io/component: "cloud-init"
              app.kubernetes.io/managed-by: "ansible-automation-platform"
              app.kubernetes.io/created-by: "{{ owner_label }}"
              database-name: "{{ normalized_db_name }}"
              database-type: "{{ selected_template.database_label }}"
              owner: "{{ owner_label }}"
            annotations:
              owner: "{{ workflow_actual_user }}"
              openshift.io/requester: "{{ workflow_actual_user }}"
              aap.redhat.com/launch-user: "{{ workflow_actual_user }}"
          type: Opaque
          stringData:
            userdata: |
              #cloud-config
              hostname: {{ vm_name }}
              manage_etc_hosts: true
              ssh_pwauth: true
              disable_root: true

              users:
                - default
                - name: {{ ssh_username }}
                  gecos: Database Administrator
                  groups:
                    - wheel
                  sudo: ALL=(ALL) NOPASSWD:ALL
                  shell: /bin/bash
                  lock_passwd: false

              chpasswd:
                expire: false
                users:
                  - name: {{ ssh_username }}
                    password: {{ ssh_password }}
                    type: text

              growpart:
                mode: auto
                devices:
                  - /
                ignore_growroot_disabled: false

              resize_rootfs: true

              runcmd:
                - systemctl enable sshd
                - systemctl restart sshd
      no_log: true

    - name: Convert template object to virtual machine definition
      ansible.builtin.set_fact:
        base_vm_definition: >-
          {{
            template_vm_object
            | to_json
            | replace('${NAME}', vm_name)
            | from_json
          }}

    - name: Define cloud-init disk
      ansible.builtin.set_fact:
        cloud_init_disk:
          name: cloudinitdisk
          disk:
            bus: virtio

    - name: Define cloud-init volume
      ansible.builtin.set_fact:
        cloud_init_volume:
          name: cloudinitdisk
          cloudInitNoCloud:
            secretRef:
              name: "{{ cloud_init_secret_name }}"

    - name: Read existing template disks and volumes
      ansible.builtin.set_fact:
        existing_disks: >-
          {{
            base_vm_definition.spec.template.spec.domain.devices.disks
            | default([])
          }}
        existing_volumes: >-
          {{
            base_vm_definition.spec.template.spec.volumes
            | default([])
          }}

    - name: Remove existing cloud-init disk from template lists
      ansible.builtin.set_fact:
        filtered_disks: >-
          {{
            existing_disks
            | rejectattr('name', 'equalto', 'cloudinitdisk')
            | list
          }}
        filtered_volumes: >-
          {{
            existing_volumes
            | rejectattr('name', 'equalto', 'cloudinitdisk')
            | list
          }}

    - name: Build final virtual machine definition
      ansible.builtin.set_fact:
        final_vm_definition: >-
          {{
            base_vm_definition
            | combine(
                {
                  'metadata': {
                    'name': vm_name,
                    'namespace': project_name,
                    'labels': (
                      base_vm_definition.metadata.labels
                      | default({})
                      | combine(
                          {
                            'app': vm_name,
                            'app.kubernetes.io/name': vm_name,
                            'app.kubernetes.io/component': 'database',
                            'app.kubernetes.io/managed-by': 'ansible-automation-platform',
                            'app.kubernetes.io/created-by': owner_label,
                            'database-name': normalized_db_name,
                            'database-type': selected_template.database_label,
                            'owner': owner_label
                          }
                        )
                    ),
                    'annotations': (
                      base_vm_definition.metadata.annotations
                      | default({})
                      | combine(
                          {
                            'database-name': db_name,
                            'database-type': selected_template.database_label,
                            'provisioned-via': 'AAP-Workflow',
                            'owner': workflow_actual_user,
                            'openshift.io/requester': workflow_actual_user,
                            'aap.redhat.com/launch-user': workflow_actual_user
                          }
                        )
                    )
                  },
                  'spec': (
                    base_vm_definition.spec
                    | combine(
                        {
                          'template': (
                            base_vm_definition.spec.template
                            | combine(
                                {
                                  'metadata': (
                                    base_vm_definition.spec.template.metadata
                                    | default({})
                                    | combine(
                                        {
                                          'labels': (
                                            base_vm_definition.spec.template.metadata.labels
                                            | default({})
                                            | combine(
                                                {
                                                  'app': vm_name,
                                                  'kubevirt.io/domain': vm_name,
                                                  'database-name': normalized_db_name,
                                                  'database-type': selected_template.database_label,
                                                  'owner': owner_label
                                                }
                                              )
                                          ),
                                          'annotations': (
                                            base_vm_definition.spec.template.metadata.annotations
                                            | default({})
                                            | combine(
                                                {
                                                  'owner': workflow_actual_user,
                                                  'openshift.io/requester': workflow_actual_user,
                                                  'aap.redhat.com/launch-user': workflow_actual_user
                                                }
                                              )
                                          )
                                        },
                                        recursive=True
                                      )
                                  ),
                                  'spec': (
                                    base_vm_definition.spec.template.spec
                                    | combine(
                                        {
                                          'domain': (
                                            base_vm_definition.spec.template.spec.domain
                                            | combine(
                                                {
                                                  'devices': (
                                                    base_vm_definition.spec.template.spec.domain.devices
                                                    | combine(
                                                        {
                                                          'disks': filtered_disks + [cloud_init_disk]
                                                        },
                                                        recursive=True
                                                      )
                                                  )
                                                },
                                                recursive=True
                                              )
                                          ),
                                          'volumes': filtered_volumes + [cloud_init_volume]
                                        },
                                        recursive=True
                                      )
                                  )
                                },
                                recursive=True
                              )
                          )
                        },
                        recursive=True
                      )
                  )
                },
                recursive=True
              )
          }}

    - name: Create database virtual machine
      kubernetes.core.k8s:
        state: present
        definition: "{{ final_vm_definition }}"

    - name: Determine root DataVolume name
      ansible.builtin.set_fact:
        root_datavolume_name: >-
          {{
            final_vm_definition.spec.dataVolumeTemplates[0].metadata.name
          }}

    - name: Wait for DataVolume creation
      kubernetes.core.k8s_info:
        api_version: cdi.kubevirt.io/v1beta1
        kind: DataVolume
        namespace: "{{ project_name }}"
        name: "{{ root_datavolume_name }}"
      register: datavolume_creation
      retries: 60
      delay: 5
      until:
        - datavolume_creation.resources | length == 1

    - name: Wait for disk import completion
      kubernetes.core.k8s_info:
        api_version: cdi.kubevirt.io/v1beta1
        kind: DataVolume
        namespace: "{{ project_name }}"
        name: "{{ root_datavolume_name }}"
      register: datavolume_status
      retries: 180
      delay: 10
      until:
        - datavolume_status.resources | length == 1
        - datavolume_status.resources[0].status is defined
        - datavolume_status.resources[0].status.phase | default('') == 'Succeeded'

    - name: Wait for virtual machine instance creation
      kubernetes.core.k8s_info:
        api_version: kubevirt.io/v1
        kind: VirtualMachineInstance
        namespace: "{{ project_name }}"
        name: "{{ vm_name }}"
      register: vmi_creation
      retries: 90
      delay: 10
      until:
        - vmi_creation.resources | length == 1

    - name: Wait for virtual machine instance readiness
      kubernetes.core.k8s_info:
        api_version: kubevirt.io/v1
        kind: VirtualMachineInstance
        namespace: "{{ project_name }}"
        name: "{{ vm_name }}"
      register: vmi_status
      retries: 120
      delay: 10
      until:
        - vmi_status.resources | length == 1
        - vmi_status.resources[0].status is defined
        - vmi_status.resources[0].status.conditions is defined
        - >-
          (
            vmi_status.resources[0].status.conditions
            | selectattr('type', 'equalto', 'Ready')
            | selectattr('status', 'equalto', 'True')
            | list
            | length
          ) > 0

    - name: Create SSH NodePort service
      kubernetes.core.k8s:
        state: present
        definition:
          apiVersion: v1
          kind: Service
          metadata:
            name: "{{ ssh_service_name }}"
            namespace: "{{ project_name }}"
            labels:
              app: "{{ vm_name }}"
              app.kubernetes.io/name: "{{ vm_name }}"
              app.kubernetes.io/component: "ssh"
              app.kubernetes.io/managed-by: "ansible-automation-platform"
              app.kubernetes.io/created-by: "{{ owner_label }}"
              database-name: "{{ normalized_db_name }}"
              database-type: "{{ selected_template.database_label }}"
              owner: "{{ owner_label }}"
            annotations:
              owner: "{{ workflow_actual_user }}"
              openshift.io/requester: "{{ workflow_actual_user }}"
              aap.redhat.com/launch-user: "{{ workflow_actual_user }}"
          spec:
            type: NodePort
            selector:
              kubevirt.io/domain: "{{ vm_name }}"
            ports:
              - name: ssh
                protocol: TCP
                port: 22
                targetPort: 22

    - name: Read SSH service
      kubernetes.core.k8s_info:
        api_version: v1
        kind: Service
        namespace: "{{ project_name }}"
        name: "{{ ssh_service_name }}"
      register: ssh_service

    - name: Store assigned SSH NodePort
      ansible.builtin.set_fact:
        ssh_node_port: "{{ ssh_service.resources[0].spec.ports[0].nodePort }}"

    - name: Read cluster nodes
      kubernetes.core.k8s_info:
        api_version: v1
        kind: Node
      register: cluster_nodes

    - name: Build worker and fallback node IP lists
      ansible.builtin.set_fact:
        worker_node_ips: >-
          {%- set addresses = [] -%}
          {%- for node in cluster_nodes.resources -%}
          {%- if node.metadata.labels['node-role.kubernetes.io/worker'] is defined -%}
          {%- for address in node.status.addresses | default([]) -%}
          {%- if address.type == 'InternalIP' -%}
          {%- set _ = addresses.append(address.address) -%}
          {%- endif -%}
          {%- endfor -%}
          {%- endif -%}
          {%- endfor -%}
          {{ addresses }}
        all_node_ips: >-
          {%- set addresses = [] -%}
          {%- for node in cluster_nodes.resources -%}
          {%- for address in node.status.addresses | default([]) -%}
          {%- if address.type == 'InternalIP' -%}
          {%- set _ = addresses.append(address.address) -%}
          {%- endif -%}
          {%- endfor -%}
          {%- endfor -%}
          {{ addresses }}

    - name: Select SSH node IP
      ansible.builtin.set_fact:
        ssh_node_ip: >-
          {{
            (
              worker_node_ips
              if worker_node_ips | length > 0
              else all_node_ips
            )
            | first
          }}

    - name: Validate SSH endpoint
      ansible.builtin.assert:
        that:
          - ssh_node_ip is defined
          - ssh_node_ip | string | length > 0
          - ssh_node_port is defined
          - ssh_node_port | string | length > 0
        fail_msg: >-
          The virtual machine was created, but the NodePort endpoint
          could not be determined.

    - name: Publish database virtual machine information
      ansible.builtin.set_stats:
        data:
          database_vm_created: true
          database_vm_project: "{{ project_name }}"
          database_vm_name: "{{ vm_name }}"
          database_vm_type: "{{ selected_vm_type }}"
          database_vm_owner: "{{ workflow_actual_user }}"
          database_name: "{{ db_name }}"
          database_ssh_username: "{{ ssh_username }}"
          database_ssh_password: "{{ ssh_password }}"
          database_ssh_host: "{{ ssh_node_ip }}"
          database_ssh_port: "{{ ssh_node_port }}"
          database_ssh_endpoint: "{{ ssh_node_ip }}:{{ ssh_node_port }}"
          database_ssh_command: >-
            ssh -p {{ ssh_node_port }} {{ ssh_username }}@{{ ssh_node_ip }}
        per_host: false
      no_log: true

    - name: Display database virtual machine access information
      ansible.builtin.debug:
        msg:
          - "============================================================"
          - "Database virtual machine provisioning completed."
          - "Project       : {{ project_name }}"
          - "Owner         : {{ workflow_actual_user }}"
          - "Database name : {{ db_name }}"
          - "Database type : {{ selected_vm_type }}"
          - "VM name       : {{ vm_name }}"
          - "SSH username  : {{ ssh_username }}"
          - "SSH password  : {{ ssh_password }}"
          - "SSH host      : {{ ssh_node_ip }}"
          - "SSH port      : {{ ssh_node_port }}"
          - "SSH endpoint  : {{ ssh_node_ip }}:{{ ssh_node_port }}"
          - "SSH command   : ssh -p {{ ssh_node_port }} {{ ssh_username }}@{{ ssh_node_ip }}"
          - "============================================================"
